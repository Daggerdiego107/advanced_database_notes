# Session – 2026-02-17

## Topics covered
- Joins: Different types of joins in SQL (INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL JOIN, CROSS JOIN, SELF JOIN) and their use cases.

## What I understood
- INNER JOIN: It returns only the rows that have matching values in both tables.
Example:
    ```sql
    SELECT columns
    FROM table1
    INNER JOIN table2
    ON table1.common_column = table2.common_column;
    ```

- LEFT JOIN: It returns all rows from the left table and the matched rows from the right table. If there is no match, the result is NULL on the right side.
Example:
    ```sql
    SELECT columns
    FROM table1
    LEFT JOIN table2
    ON table1.common_column = table2.common_column;
    ```
- RIGHT JOIN: It returns all rows from the right table and the matched rows from the left table. If there is no match, the result is NULL on the left side.
Example:
    ```sql
    SELECT columns
    FROM table1
    RIGHT JOIN table2
    ON table1.common_column = table2.common_column;
    ```
- FULL JOIN: It returns all rows when there is a match in either left or right table. If there is no match, the result is NULL on the side that does not have a match.
Example:
    ```sql
    SELECT columns
    FROM table1
    FULL JOIN table2
    ON table1.common_column = table2.common_column;
    ```

- CROSS JOIN: It returns the Cartesian product of the two tables, meaning it returns all possible combinations of rows from both tables.
Example:
    ```sql
    SELECT columns
    FROM table1
    CROSS JOIN table2;
    ```
- SELF JOIN: It is a regular join but the table is joined with itself. It is used to compare rows within the same table.
Example:
    ```sql
    SELECT t1.columns, t2.columns
    FROM table t1
    JOIN table t2 ON t1.common_column = t2.common_column;
    ```

## What is still confusing
- When to use each type of join in real-world scenarios and how to optimize queries that involve multiple joins.

- The performance implications of different join types and how to choose the most efficient one for a given query.

## Questions
- Why?

- How do different database systems implement joins, and are there any differences in performance or behavior?

- Can you provide examples of when to use each type of join in practical applications?

## Related concepts
- [Concept name](../concepts/concept-name.md)

## Resources used
- See `resources/articles.md`
